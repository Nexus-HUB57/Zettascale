/**
 * ARQUIVO NEUTRALIZADO - MODO SOBERANO
 * A lógica de persistência institucional agora reside exclusivamente em:
 * ./persistence-service.ts (Versão Server Action)
 * ../../scripts/restore-seal.js (Versão CLI)
 * 
 * Este arquivo foi desativado para evitar conflitos de bundle no navegador.
 */
export const getPersistedSeal = async () => null;
export const persistSovereignSeal = async () => ({ success: false });
